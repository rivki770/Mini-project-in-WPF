<UserControl x:Class="PL.Controls.UnitHostingCtrl" FlowDirection="RightToLeft"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="155" d:DesignWidth="700">
    <Grid Name="hostUnitGrid" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <Label Content="{Binding Path=HostingUnitName}"  FontSize="20" Grid.Column="0" HorizontalAlignment="Left" Height="41" Margin="10,10,0,0" VerticalAlignment="Top" Width="204"/>
        <Label Content="{Binding Path=Rooms}"  FontSize="20" Grid.Column="0" HorizontalAlignment="Left" Height="41" Margin="10,73,0,0" VerticalAlignment="Top" Width="204"/>

        <Image Source="{Binding Path=Images[0]}" Grid.Column="2" />
        <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" BorderBrush="#61738B" BorderThickness="1" />
    </Grid>
</UserControl>
